function print_time_warning(warning)
%#codegen

% รับค่า warning จาก input
% warning คือสัญญาณ boolean ที่บ่งบอกว่ามีการเตือนหรือไม่

if warning
    % ดึงเวลาปัจจุบัน
    current_time = datetime('now');
    
    % พิมพ์เวลาปัจจุบันพร้อมข้อความ
    fprintf('[%s] Published crash warning: %d\n', datestr(current_time, 'yyyy-mm-dd HH:MM:SS'), warning);
end
end
